# Reviewed submissions

Copy the fictional example here only after replacing every value with a real, dated, source-by-source review. The filename must match the JSON `id`. Accepted files are rendered into `RANKINGS.md`.
